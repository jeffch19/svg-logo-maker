//Create SVG Generation Module
function generateSVG({ text, textColor, shape, shapeColor }) {
  
  const svgContent = `
    <svg width="300" height="200">
      <!-- Your SVG content here -->
    </svg>
  `;
  return svgContent;
}

module.exports = generateSVG;
